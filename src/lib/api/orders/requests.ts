/**
 * This file was auto-generated by scripts/generate-api-types.ts.
 * Do not make direct changes to the file.
 */

import { apiClient } from "../client";
import { ApiError } from "../envelope";
import type { FetchOptions } from "openapi-fetch";
import type { paths } from "../schema.d.ts";
import type { RequestOptions } from "../shared/types";

import { API_ORDERS_PATHS } from "./endpoints";

export async function createOrder(
  ...[options]: RequestOptions<FetchOptions<paths[typeof API_ORDERS_PATHS.ORDERS]["post"]>>
) {
  const { data, error, response } = await apiClient.POST(API_ORDERS_PATHS.ORDERS, options as FetchOptions<paths[typeof API_ORDERS_PATHS.ORDERS]["post"]>);
  const status = response.status;
  if (error) {
    const errObj = error as unknown as { error?: { message?: string }; message?: string };
    const msg = errObj?.error?.message ?? errObj?.message ?? "API Error";
    throw new ApiError(msg, status, errObj);
  }
  return data;
}
